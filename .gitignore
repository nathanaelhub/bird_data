# Python
.venv/
__pycache__/
*.pyc

# Data is regenerated by etl.py, not committed
# (raw is fetched from source; processed is derived — both under data/*/.gitkeep)
data/raw/*.csv
data/processed/*.csv

# macOS
.DS_Store
